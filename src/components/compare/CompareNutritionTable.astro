---
---
<div id="compare-table-container" class="hidden bg-pure-card rounded-[var(--radius-card)] shadow-sm border border-border-soft overflow-x-auto custom-scrollbar luxury-card mb-8">
  <table class="min-w-full divide-y divide-border-soft">
    <thead id="compare-table-head" class="bg-slate-50">
      <!-- Injected -->
    </thead>
    <tbody id="compare-table-body" class="bg-white divide-y divide-border-soft">
      <!-- Injected -->
    </tbody>
  </table>
</div>
